# AllRede/comments/urls.py

from django.urls import path
from . import views # You might need this later if you add views to comments app

urlpatterns = [
    # Add your comment-specific URL patterns here later, e.g.:
    # path('some_path/', views.some_view, name='some_name'),
]